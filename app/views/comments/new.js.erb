$('.row.comments').slideUp().addClass('hide');
$(".row[data-rev-id=<%= @review.id %>]").slideDown().removeClass('hide').first().empty().append("<%=j render :partial => 'comments/form', :locals => {:id => @review.id } %>");
